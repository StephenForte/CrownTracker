# Required for the web app. Use a long, random string in production.
APP_PASSWORD=change-me
SESSION_SECRET=replace-with-at-least-32-random-characters

# Render provides this automatically from the blueprint. For local development:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/crown_tracker
